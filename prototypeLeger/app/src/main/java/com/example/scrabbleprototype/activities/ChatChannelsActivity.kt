package com.example.scrabbleprototype.activities

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.scrabbleprototype.R

class ChatChannelsActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_all_chat_channels)
    }



        /*val channelsButton = findViewById<TextView>(R.id.channels_button)
        val channel1Players = setOf("player1c1", "player2c1", "player3c1")
        val channel2Players = setOf("player1c2", "player2c2", "player3c2")
        val channel3Players = setOf("player1c3", "player2c3", "player3c3")
        val channels = mapOf(
            "channel1" to channel1Players,
            "channel2" to channel2Players,
            "channel3" to channel3Players
        )*/

}
